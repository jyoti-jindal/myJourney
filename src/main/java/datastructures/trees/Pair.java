package datastructures.trees;

public class Pair {
    public TreeNode treeNode;
    public int dist;

    public Pair(TreeNode treeNode, int dist) {
        this.treeNode = treeNode;
        this.dist = dist;
    }
}